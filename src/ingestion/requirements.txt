# ATALAYA // Ingesta — dependencias de runtime
#
# El conector de referencia (misp_stix_connector.py) corre con la librería
# estándar únicamente, a propósito: es el contrato canónico del formato y
# tiene que poder ejecutarse en un contenedor pelado.
#
# Lo de abajo es lo que necesitan los conectores que hablan con APIs reales,
# y nada más. Cada dependencia que se declara es una que hay que auditar y
# parchear para siempre: pymisp, stix2 y stix2-validator estaban acá sin que
# una sola línea de código las importara.
httpx==0.28.1
tenacity==9.1.4
python-dotenv==1.2.3

# Persistencia compartida con la API (misiones y avistamientos).
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
