# ATALAYA // herramientas de desarrollo y CI
-r requirements.txt
pytest==9.0.3
# Sin pytest-asyncio: la suite no tiene un solo `async def test_`. Todo el
# testing asíncrono pasa por helpers con asyncio.run() y una conexión propia,
# que además prueba que el dato está en la base y no sólo en la sesión de la
# app. Una dependencia menos que mantener parcheada.
flake8==7.1.1
black==26.3.1
bandit==1.8.2

# Postgres real y efímero para los tests: nada de SQLite. Si el motor de
# los tests difiere del de producción, los tests mienten.
pgserver==0.1.4
