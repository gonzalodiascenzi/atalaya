# ATALAYA // Ingesta — dependencias
#
# El conector de referencia (misp_stix_connector.py) corre con la librería
# estándar únicamente, a propósito: es el contrato canónico del formato y
# tiene que poder ejecutarse en un contenedor pelado.
#
# Los conectores que hablan con APIs reales sí necesitan lo de abajo.
httpx==0.28.1
tenacity==9.0.0
python-dotenv==1.0.1

# Persistencia compartida con la API (misiones y avistamientos).
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# Publicación opcional hacia las plataformas de correlación.
pymisp==2.5.3
pycti==6.4.6
stix2==3.0.1
stix2-validator==3.2.0
